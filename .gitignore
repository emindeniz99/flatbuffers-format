# Scoped to the FlatBuffers workspace — the repo root .gitignore only
# carries repo-wide junk (OS/editor files); Node build state is this
# project's own concern.
node_modules/
dist/
build/
deploy/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
