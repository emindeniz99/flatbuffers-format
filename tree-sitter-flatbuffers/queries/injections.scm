; No language injections for FlatBuffers schemas — string literals
; carry filenames (include paths), file extensions, file identifiers,
; or metadata values. None contain another language's source.
;
; Kept as a placeholder so editor consumers that look for the file
; (Helix, Zed) don't error out.
