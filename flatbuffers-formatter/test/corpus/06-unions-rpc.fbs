table A { x: int; }

table B { y: string; }

table C { z: bool; }

union U { A, alias: B, C }

rpc_service S {
  M1(A): B;
  M2(B): C (streaming: "server");
  M3(C): A (idempotent, streaming: "none");
}
